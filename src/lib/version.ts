/**
 * Current build version of the application.
 * Increment on every deploy that requires a client-side cache reset.
 */
export const APP_VERSION = "1.1.0";

export const VERSION_STORAGE_KEY = "app_version";
